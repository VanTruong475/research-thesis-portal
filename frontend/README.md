# Research Thesis Portal Frontend

Angular frontend foundation for the Research Thesis Portal.

## Development

```bash
npm install
npm start
```

## Build

```bash
npm run build
```

The backend API base URL is configured in `src/environments/environment*.ts` and defaults to `/api/v1`.
